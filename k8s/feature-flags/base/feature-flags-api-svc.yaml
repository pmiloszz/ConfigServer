apiVersion: v1
kind: Service
metadata:
  name: feature-flags-api-svc
  namespace: feature-flags
  labels:
    app: feature-flags-api
spec:
  type: ClusterIP
  selector:
    app: feature-flags-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

