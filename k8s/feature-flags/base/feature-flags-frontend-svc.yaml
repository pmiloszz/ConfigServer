apiVersion: v1
kind: Service
metadata:
  name: feature-flags-svc
  namespace: feature-flags
  labels:
    app: feature-flags-frontend
spec:
  type: ClusterIP
  selector:
    app: feature-flags-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP

