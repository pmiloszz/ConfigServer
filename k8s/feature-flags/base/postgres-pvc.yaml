apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: feature-flags
spec:
  accessModes:
    - ReadWriteOnce
  # Use the cluster default StorageClass (portable across clusters).
  resources:
    requests:
      storage: 2Gi

